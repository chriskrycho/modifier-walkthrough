<div>
  <p>current value: {{display @value}}</p>
  <label>
    {{@label}}
    <input value={{@value}} {{on "input" (pick "target.value" @updateValue)}}>
  </label>

  <button {{on "click" @delete}}>delete this one!</button>
</div>